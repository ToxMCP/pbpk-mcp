# PBPK MCP v0.2.1

Suggested GitHub release title:

- `PBPK MCP v0.2.1: README cleanup and release metadata alignment`

Suggested short release summary:

- `v0.2.1` is a documentation-focused patch release that cleans up the public README, fixes Mermaid rendering, and aligns package and repository metadata with the `ToxMCP/pbpk-mcp` publication state.

## Highlights

- cleaner GitHub-facing README structure modeled on the broader ToxMCP project layout
- fixed Mermaid architecture rendering in the public docs
- clarified supported runtime formats, conversion-only formats, and OECD-oriented limitations
- aligned version and repository metadata across package files, compose config, and generated docs

## Important Notes

- no new MCP tools were added in `v0.2.1`
- no runtime behavior changed relative to `v0.2.0`
- this patch is intended to make the published repository easier to understand and safer to cite

## Suggested Release Notes Body

```markdown
PBPK MCP `v0.2.1` is a documentation and release-metadata patch over `v0.2.0`.

This release:

- restructures the public README into a cleaner project overview
- fixes Mermaid rendering for the architecture diagram on GitHub
- clarifies direct runtime support versus conversion-only formats
- aligns package, compose, and generated metadata with the published `ToxMCP/pbpk-mcp` repository

There are no runtime or MCP API behavior changes in this patch release.
```
